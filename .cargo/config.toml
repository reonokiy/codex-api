[net]
git-fetch-with-cli = true

[alias]
setup = "run --locked -p xtask -- setup"
test-all = "test --workspace --locked -- --include-ignored"
e2e = "test --locked --test live -- --ignored --test-threads=1"
